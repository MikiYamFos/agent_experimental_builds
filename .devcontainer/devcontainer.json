{
  "name": "agent-experimental-builds",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "postCreateCommand": "python -m pip install --upgrade pip && pip install uv && uv sync || true",
  "remoteEnv": {
    "AWS_REGION": "us-east-1"
  }
}